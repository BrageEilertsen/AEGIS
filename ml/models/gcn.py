"""GCN — baseline model (spec §7.3, step 1).

Establishes the pipeline and a reference metric. Phase 0: placeholder. Implemented in Phase 2.
"""
from __future__ import annotations

# TODO (Phase 2): class GCN(torch.nn.Module) with a common forward(data) -> logits interface.
