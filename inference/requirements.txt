# AEGIS inference service (spec §8.2). Also needs the repo's root requirements.txt (torch cu121,
# torch_geometric, scipy, scikit-learn, pandas, networkx) and the ml/ package on PYTHONPATH.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
# Grounded plain-English explanation summaries via a small local HF model (CPU).
transformers>=4.44
