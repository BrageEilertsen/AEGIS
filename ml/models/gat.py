"""GAT — Graph Attention Network, the primary model (spec §7.3, step 3).

Attention weights over edges double as a native explanation signal feeding the explainability
subsystem (§7.7). Phase 0: placeholder. Implemented in Phase 3.
"""
from __future__ import annotations

# TODO (Phase 3): class GAT(torch.nn.Module) exposing per-edge attention via the common interface.
